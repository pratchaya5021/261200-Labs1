// No code to write or modify for this task. Read article on the right.

public class Task {
  public double dividend = 50.0;
  public double divisor = 5.0;
  public string divide() {
      if (divisor = 0) // runtime error (however most IDEs might prevent you from compiling and suggest you to fix it)
          return "Divide by zero"; // syntax error (the program will not compile unless it is fixed)
      else
          return (string) dividend/divisor;
  }
}